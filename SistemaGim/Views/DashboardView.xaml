<UserControl x:Class="SistemaGim.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="Transparent"
             FontFamily="{DynamicResource MaterialDesignDefaultFont}"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Cabecera -->
            <RowDefinition Height="Auto"/>
            <!-- Tarjetas KPI -->
            <RowDefinition Height="*"/>
            <!-- Gráficos -->
        </Grid.RowDefinitions>

        <!-- 1. CABECERA -->
        <Grid Grid.Row="0" Margin="0 0 0 20">
            <TextBlock Text="Resumen Estadístico" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="BtnActualizar" Click="BtnActualizar_Click" Style="{StaticResource MaterialDesignOutlinedButton}" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <TextBlock Text="Actualizar Datos" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- 2. TARJETAS DE INDICADORES (CON ICONOS) -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0 0 0 20">
            <!-- Ingresos Hoy -->
            <materialDesign:Card Margin="10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                <Grid Margin="20 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Ingresos Hoy" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtPagosHoy" Text="C$ 0.00" FontSize="26" FontWeight="Bold" Margin="0 5 0 0" Foreground="#4CAF50"/>
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="CashMultiple" Width="45" Height="45" Foreground="#4CAF50" Opacity="0.2" VerticalAlignment="Center"/>
                </Grid>
            </materialDesign:Card>

            <!-- Ingresos Mes -->
            <materialDesign:Card Margin="10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                <Grid Margin="20 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Ingresos Mes" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtPagosMes" Text="C$ 0.00" FontSize="26" FontWeight="Bold" Margin="0 5 0 0" Foreground="#4CAF50"/>
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="Bank" Width="45" Height="45" Foreground="#4CAF50" Opacity="0.2" VerticalAlignment="Center"/>
                </Grid>
            </materialDesign:Card>

            <!-- Visitas Hoy -->
            <materialDesign:Card Margin="10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                <Grid Margin="20 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Visitas Hoy" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtVisitasHoy" Text="0" FontSize="26" FontWeight="Bold" Margin="0 5 0 0" Foreground="#2196F3"/>
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="AccountClock" Width="45" Height="45" Foreground="#2196F3" Opacity="0.2" VerticalAlignment="Center"/>
                </Grid>
            </materialDesign:Card>

            <!-- Visitas Mes -->
            <materialDesign:Card Margin="10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                <Grid Margin="20 15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Visitas Mes" FontSize="14" Foreground="Gray" FontWeight="SemiBold"/>
                        <TextBlock x:Name="TxtVisitasMes" Text="0" FontSize="26" FontWeight="Bold" Margin="0 5 0 0" Foreground="#2196F3"/>
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="CalendarMonth" Width="45" Height="45" Foreground="#2196F3" Opacity="0.2" VerticalAlignment="Center"/>
                </Grid>
            </materialDesign:Card>
        </UniformGrid>

        <!-- 3. SECCIÓN DE GRÁFICOS -->
        <Grid Grid.Row="2" Margin="10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <!-- Gráfico de Barras -->
                <ColumnDefinition Width="4*"/>
                <!-- Gráfico de Pastel -->
            </Grid.ColumnDefinitions>

            <!-- Gráfico de Barras: Ingresos de los últimos 7 días -->
            <materialDesign:Card Grid.Column="0" Margin="0 0 10 0" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Ingresos de los últimos 7 días" FontSize="16" FontWeight="SemiBold" Foreground="Gray" Margin="0 0 0 15"/>

                    <lvc:CartesianChart x:Name="GraficoBarras" Grid.Row="1">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis x:Name="EjeFechas" Title="Fechas" />
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Monto (C$)" LabelFormatter="{Binding FormatoMoneda}" />
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </materialDesign:Card>

            <!-- Gráfico de Pastel: Distribución de Ingresos -->
            <materialDesign:Card Grid.Column="1" Margin="10 0 0 0" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Ingresos por Concepto (Mes)" FontSize="16" FontWeight="SemiBold" Foreground="Gray" Margin="0 0 0 15"/>

                    <lvc:PieChart x:Name="GraficoPastel" Grid.Row="1" InnerRadius="40" LegendLocation="Bottom" Hoverable="True">
                        <!-- El gráfico se llena desde C# -->
                    </lvc:PieChart>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>