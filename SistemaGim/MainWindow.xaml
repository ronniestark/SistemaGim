<Window x:Class="SistemaGim.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Gym Manager Pro" Height="720" Width="1080"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignDefaultFont}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <!-- Menú Lateral -->
            <ColumnDefinition Width="*"/>
            <!-- Contenido Principal -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignDarkBackground}">
            <StackPanel Margin="0 20">
                <TextBlock Text="GYM MANAGER" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 0 0 30"/>

                <!-- Agregamos el evento Click aquí -->
                <Button Click="BtnDashboard_Click" Style="{StaticResource MaterialDesignFlatLightBgButton}" Content="Dashboard" Margin="10 5" HorizontalAlignment="Stretch" />

                <Button Style="{StaticResource MaterialDesignFlatLightBgButton}" Content="Visitas Diarias" Margin="10 5" HorizontalAlignment="Stretch"/>
                <Button Style="{StaticResource MaterialDesignFlatLightBgButton}" Content="Miembros y Membresías" Margin="10 5" HorizontalAlignment="Stretch"/>
                <Button Style="{StaticResource MaterialDesignFlatLightBgButton}" Content="Reportes" Margin="10 5" HorizontalAlignment="Stretch"/>
                <Button Style="{StaticResource MaterialDesignFlatLightBgButton}" Content="Configuración" Margin="10 5" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>

        <!-- Contenedor Dinámico (Aquí se cargan las pantallas) -->
        <ContentControl Grid.Column="1" x:Name="MainContent" Margin="20"/>
    </Grid>
</Window>